global class ScheduleBatchDNCaseExecuteRules implements Schedulable {
	global void execute(SchedulableContext sc) {
		//BatchDNCaseExecuteRules batch = new BatchDNCaseExecuteRules(); 
		//Database.executeBatch(batch, 5);
	}
}