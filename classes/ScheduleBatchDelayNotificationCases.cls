global class ScheduleBatchDelayNotificationCases implements Schedulable {
   global void execute(SchedulableContext sc) {
          //BatchDelayNotificationCases batch = new BatchDelayNotificationCases(); 
          //Database.executeBatch(batch, 5);
   }
}